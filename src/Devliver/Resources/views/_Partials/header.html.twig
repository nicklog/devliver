<header>
    <nav class="navbar navbar-expand-md navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="{{ path('devliver_index') }}"><i class="fas fa-cubes fa-fw"></i> Devliver</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                {% if is_granted('ROLE_MANAGER') %}
                    {{ knp_menu_render('main_left', {
                        template: '@Devliver/_Partials/knp_bootstrap4.html.twig',
                        currentClass: 'active',
                    }) }}

                    <form class="form-inline ml-auto mr-3">
                        {% if latest_release.name != current_release.name and is_granted('ROLE_USER') %}
                            <div class="btn btn-warning" data-toggle="tooltip" title="{{ latest_release.name }}">
                                Update Available
                            </div>
                        {% endif %}
                    </form>

                    {{ knp_menu_render('main_right', {
                        template: '@Devliver/_Partials/knp_bootstrap4.html.twig',
                        currentClass: 'active',
                    }) }}
                {% endif %}
            </div>
        </div>
    </nav>
</header>
