<header class="navbar navbar-expand-md navbar-dark d-print-none">
    <div class="container">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <a class="navbar-brand navbar-brand-autodark d-none-navbar-horizontal pe-0 pe-md-3" href="{{ path('app_index') }}">
            <span class="brand-text font-weight-light"><i class="fas fa-cubes fa-fw"></i> Devliver</span>
        </a>

        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <div class="navbar-nav flex-row order-md-last">
                <div class="nav-item">
                    <a class="nav-link" href="{{ path('app_howto') }}">
                        <i class="fas fa-question-circle fa-fw"></i>
                        How-To
                    </a>
                </div>
                <div class="nav-item dropdown" >
                    <a href="#" class="dropdown-toggle nav-link" data-bs-toggle="dropdown">
                        <i class="fas fa-user fa-fw"></i>
                        {{ app.user.username }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right">
                        <a href="{{ path('app_profile_index') }}" class="dropdown-item">
                            <i class="fas fa-user fa-fw"></i>
                            Profile
                        </a>
                        <a href="{{ path('app_profile_password') }}" class="dropdown-item">
                            <i class="fas fa-key fa-fw"></i>
                            Change password
                        </a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('app_logout') }}" class="dropdown-item">
                            <i class="fas fa-sign-out-alt fa-fw"></i>
                            Logout
                        </a>
                    </div>
                </div>
            </div>
        {% endif %}
    </div>
</header>

{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
    <div class="navbar-expand-md">
        <div class="collapse navbar-collapse" id="navbar-menu">
            <div class="navbar navbar-light">
                <div class="container-xl">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_package_index') }}">
                                <i class="fas fa-cube fa-fw"></i>
                                Packages
                            </a>
                        </li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_user_index') }}">
                                    <i class="fas fa-users fa-fw"></i>
                                    Users
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_client_index') }}">
                                    <i class="fas fa-user-shield fa-fw"></i>
                                    Clients
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endif %}